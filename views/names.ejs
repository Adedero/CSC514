<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Names</title>
</head>

<body class="p-5">
    <% customers.forEach(customer=> { %>
        <main class="grid gap-5 uppercase">
            <p><%= `${customer.lastName} ${customer.firstName}` %></p>
        </main>
        <% }); %>
</body>

</html>